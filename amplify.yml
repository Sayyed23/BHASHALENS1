version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 'echo "Phase: preBuild"'
        - 'if [ ! -d "$AWS_SRC_DIR/flutter" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 $AWS_SRC_DIR/flutter; fi'
        - 'export PATH="$PATH:$AWS_SRC_DIR/flutter/bin"'
        - 'flutter config --no-analytics'
        - 'flutter precache --web'
        - 'flutter config --enable-web'
        - 'cd $AWS_SRC_DIR/bhashalens_app && flutter pub get'
    build:
      commands:
        - 'echo "Phase: build"'
        - 'export PATH="$PATH:$AWS_SRC_DIR/flutter/bin"'
        - 'cd $AWS_SRC_DIR/bhashalens_app && flutter build web --release'
  artifacts:
    baseDirectory: bhashalens_app/build/web
    files:
      - '**/*'
  cache:
    paths:
      - 'flutter/.pub-cache/**/*'
      - 'flutter/bin/cache/**/*'
      - 'bhashalens_app/.pub-cache/**/*'
